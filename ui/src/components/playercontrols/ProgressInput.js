import React, { useState, useEffect, useContext } from 'react'; 
import styled from 'styled-components';

const ProgressInput = () => {

    // same as ProgressBar.js 
    // but as a draggable slider input
}



const Wrapper = styled.div`
    
`;